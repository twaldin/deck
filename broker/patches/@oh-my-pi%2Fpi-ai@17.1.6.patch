diff --git a/node_modules/@oh-my-pi/pi-ai/.bun-tag-a4801a0d4094ec35 b/.bun-tag-a4801a0d4094ec35
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@oh-my-pi/pi-ai/.bun-tag-cefee13a3ece00c2 b/.bun-tag-cefee13a3ece00c2
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/types/auth-storage.d.ts b/dist/types/auth-storage.d.ts
index 1eb5bd8a5bc18f14c95ac819c5af566fa42f7886..2469787707c0e583bcd55eff7f6aa93d9e945d3c 100644
--- a/dist/types/auth-storage.d.ts
+++ b/dist/types/auth-storage.d.ts
@@ -881,9 +881,14 @@ export declare class AuthStorage {
     /** Record one observed provider request cost for later local usage aggregation. */
     recordUsageCost(provider: Provider, costUsd: number, options?: {
         sessionId?: string;
+        credentialId?: number;
         recordedAt?: number;
         baseUrl?: string;
+        model?: string;
+        serviceTier?: UsageCostHistoryEntry["serviceTier"];
     }): boolean;
+    /** Recorded request costs, oldest first. */
+    listUsageCosts(query?: UsageCostHistoryQuery): UsageCostHistoryEntry[];
     /**
      * Forward one completed request's usage to the store's observer hook.
      * Broker-backed stores batch these into per-install reports so the broker
diff --git a/dist/types/usage.d.ts b/dist/types/usage.d.ts
index f5a8743f82daf94cc4d0f4cc1bd926830ef94b56..68421a93623224b356dc6adae4d7058746dbc006 100644
--- a/dist/types/usage.d.ts
+++ b/dist/types/usage.d.ts
@@ -146,8 +146,12 @@ export interface UsageCostHistoryEntry {
     provider: Provider;
     /** Stable credential identity key (account/email/project/secret derived). */
     accountKey: string;
-    /** Estimated request cost in USD. */
+    /** Estimated request cost at the model's Standard rate, in USD. */
     costUsd: number;
+    /** Model that served the request, when recorded by the gateway. */
+    model?: string;
+    /** Effective attributed tier. Only supported ChatGPT Fast requests are priority. */
+    serviceTier?: "default" | "priority";
 }
 /** Filter for reading observed request costs. */
 export interface UsageCostHistoryQuery {
diff --git a/src/auth-storage.ts b/src/auth-storage.ts
index 51b9f25ac19c50d380ef3ce6a3ceb20f926d6c12..1ab9122c3e832d639ad63ecfd8e3c710ef9554b0 100644
--- a/src/auth-storage.ts
+++ b/src/auth-storage.ts
@@ -3184,18 +3184,27 @@ export class AuthStorage {
 	recordUsageCost(
 		provider: Provider,
 		costUsd: number,
-		options?: { sessionId?: string; recordedAt?: number; baseUrl?: string },
+		options?: {
+			sessionId?: string;
+			credentialId?: number;
+			recordedAt?: number;
+			baseUrl?: string;
+			model?: string;
+			serviceTier?: UsageCostHistoryEntry["serviceTier"];
+		},
 	): boolean {
 		if (!Number.isFinite(costUsd) || costUsd <= 0) return false;
 		const record = this.#store.recordUsageCosts;
 		if (!record) return false;
-		const credential = this.#resolveObservedUsageCredential(provider, options?.sessionId);
+		const credential = this.#resolveObservedUsageCredential(provider, options?.sessionId, options?.credentialId);
 		if (!credential) return false;
 		const entry: UsageCostHistoryEntry = {
 			recordedAt: options?.recordedAt ?? Date.now(),
 			provider,
 			accountKey: this.#buildUsageCacheIdentity(credential),
 			costUsd,
+			model: options?.model,
+			serviceTier: options?.serviceTier,
 		};
 		try {
 			record.call(this.#store, [entry]);
@@ -3216,6 +3225,11 @@ export class AuthStorage {
 		}
 	}
 
+	/** Recorded request costs, oldest first. */
+	listUsageCosts(query?: UsageCostHistoryQuery): UsageCostHistoryEntry[] {
+		return this.#store.listUsageCosts?.(query) ?? [];
+	}
+
 	/**
 	 * Forward one completed request's usage to the store's observer hook.
 	 * Broker-backed stores batch these into per-install reports so the broker
@@ -3266,8 +3280,15 @@ export class AuthStorage {
 		return this.#store.getClientUsageSummary?.(sinceMs) ?? { clients: [] };
 	}
 
-	#resolveObservedUsageCredential(provider: Provider, sessionId?: string): UsageCredential | undefined {
+	#resolveObservedUsageCredential(provider: Provider, sessionId?: string, credentialId?: number): UsageCredential | undefined {
 		const entries = this.#getStoredCredentials(provider);
+		if (credentialId !== undefined) {
+			const explicit = entries.find(entry => entry.id === credentialId)?.credential;
+			if (explicit === undefined) return undefined;
+			return explicit.type === "api_key"
+				? { type: "api_key", apiKey: explicit.key }
+				: this.#buildUsageCredential(explicit);
+		}
 		const sessionCredential = this.#getSessionCredential(provider, sessionId);
 		if (sessionCredential) {
 			const credential = entries[sessionCredential.index]?.credential;
@@ -6781,10 +6802,10 @@ export class SqliteAuthCredentialStore implements AuthCredentialStore {
 			"SELECT recorded_at, provider, account_key, email, account_id, limit_id, label, window_label, used_fraction, status, resets_at FROM usage_history WHERE recorded_at >= ? AND (? IS NULL OR provider = ?) ORDER BY recorded_at ASC",
 		);
 		this.#insertUsageCostStmt = this.#db.prepare(
-			"INSERT INTO usage_cost_history (recorded_at, provider, account_key, cost_usd) VALUES (?, ?, ?, ?)",
+			"INSERT INTO usage_cost_history (recorded_at, provider, account_key, cost_usd, model, service_tier) VALUES (?, ?, ?, ?, ?, ?)",
 		);
 		this.#listUsageCostsStmt = this.#db.prepare(
-			"SELECT recorded_at, provider, account_key, cost_usd FROM usage_cost_history WHERE recorded_at >= ? AND (? IS NULL OR provider = ?) AND (? IS NULL OR account_key = ?) ORDER BY recorded_at ASC",
+			"SELECT recorded_at, provider, account_key, cost_usd, model, service_tier FROM usage_cost_history WHERE recorded_at >= ? AND (? IS NULL OR provider = ?) AND (? IS NULL OR account_key = ?) ORDER BY recorded_at ASC",
 		);
 	}
 
@@ -6884,7 +6905,9 @@ export class SqliteAuthCredentialStore implements AuthCredentialStore {
 				recorded_at INTEGER NOT NULL,
 				provider TEXT NOT NULL,
 				account_key TEXT NOT NULL,
-				cost_usd REAL NOT NULL
+				cost_usd REAL NOT NULL,
+				model TEXT,
+				service_tier TEXT
 			);
 			CREATE INDEX IF NOT EXISTS idx_usage_cost_history_lookup ON usage_cost_history(provider, account_key, recorded_at);
 			CREATE INDEX IF NOT EXISTS idx_usage_history_recorded ON usage_history(recorded_at);
@@ -6911,6 +6934,19 @@ export class SqliteAuthCredentialStore implements AuthCredentialStore {
 			CREATE INDEX IF NOT EXISTS idx_client_usage_recorded ON client_usage(recorded_at);
 		`);
 
+		const usageCostColumns = this.#db.prepare("PRAGMA table_info(usage_cost_history)");
+		try {
+			const columns = usageCostColumns.all() as Array<{ name?: string }>;
+			if (!columns.some(column => column.name === "model")) {
+				this.#db.run("ALTER TABLE usage_cost_history ADD COLUMN model TEXT");
+			}
+			if (!columns.some(column => column.name === "service_tier")) {
+				this.#db.run("ALTER TABLE usage_cost_history ADD COLUMN service_tier TEXT");
+			}
+		} finally {
+			usageCostColumns.finalize();
+		}
+
 		if (!this.#authCredentialsTableExists()) {
 			this.#createAuthCredentialsTable();
 			this.#createAuthCredentialBlocksTable();
@@ -7686,7 +7722,7 @@ export class SqliteAuthCredentialStore implements AuthCredentialStore {
 	recordUsageCosts(entries: UsageCostHistoryEntry[]): void {
 		try {
 			for (const entry of entries) {
-				this.#insertUsageCostStmt.run(entry.recordedAt, entry.provider, entry.accountKey, entry.costUsd);
+				this.#insertUsageCostStmt.run(entry.recordedAt, entry.provider, entry.accountKey, entry.costUsd, entry.model ?? null, entry.serviceTier ?? null);
 			}
 		} catch {
 			// Cost history is best-effort; never break request persistence.
@@ -7708,12 +7744,16 @@ export class SqliteAuthCredentialStore implements AuthCredentialStore {
 				provider: string;
 				account_key: string;
 				cost_usd: number;
+				model: string | null;
+				service_tier: string | null;
 			}>;
 			return rows.map(row => ({
 				recordedAt: row.recorded_at,
 				provider: row.provider as Provider,
 				accountKey: row.account_key,
 				costUsd: row.cost_usd,
+				model: row.model ?? undefined,
+				serviceTier: (row.service_tier ?? undefined) as UsageCostHistoryEntry["serviceTier"],
 			}));
 		} catch {
 			return [];
diff --git a/src/providers/openai-responses-server.ts b/src/providers/openai-responses-server.ts
index db60dc4665fe441f815127bed5da4d47b3246b6d..213cb4d19b05b51488b4f2f3231860d2e49ecbd4 100644
--- a/src/providers/openai-responses-server.ts
+++ b/src/providers/openai-responses-server.ts
@@ -266,15 +266,24 @@ function buildTools(tools: Array<OpenAIResponsesTool | { type: string }> | undef
 	return out.length > 0 ? out : undefined;
 }
 
+function responseModelIdentity(modelId: string): Pick<AssistantMessage, "api" | "provider" | "model"> {
+	const modelParts = modelId.split("/");
+	const provider = modelParts.length > 1 ? modelParts.at(-2)! : "openai";
+	return {
+		api: provider === "openai-codex" ? "openai-codex-responses" : "openai-responses",
+		provider,
+		model: modelParts.at(-1) ?? modelId,
+	};
+}
+
 function ensureAssistantPlaceholder(messages: Message[], modelId: string, now: number): AssistantMessage {
 	const last = messages[messages.length - 1];
 	if (last && last.role === "assistant") return last;
+	const identity = responseModelIdentity(modelId);
 	const placeholder: AssistantMessage = {
 		role: "assistant",
 		content: [],
-		api: "openai-responses",
-		provider: "openai",
-		model: modelId,
+		...identity,
 		usage: {
 			input: 0,
 			output: 0,
@@ -290,6 +299,19 @@ function ensureAssistantPlaceholder(messages: Message[], modelId: string, now: n
 	return placeholder;
 }
 
+function appendNativeHistoryItem(assistant: AssistantMessage, item: unknown): void {
+	const payload = assistant.providerPayload?.type === "openaiResponsesHistory"
+		? assistant.providerPayload
+		: {
+			type: "openaiResponsesHistory" as const,
+			provider: assistant.provider,
+			dt: true,
+			items: [],
+		};
+	payload.items.push(structuredCloneJSON(item) as unknown as Record<string, unknown>);
+	assistant.providerPayload = payload;
+}
+
 /** Flatten a function_call_output array form (text + refusal) into a single string. */
 function flattenFunctionOutputArray(blocks: readonly unknown[]): string {
 	const parts: string[] = [];
@@ -383,12 +405,17 @@ export function parseRequest(body: unknown, headers?: Headers): ParsedRequest {
 							id: msg.id,
 							phase: msg.phase,
 						});
+						const identity = responseModelIdentity(data.model);
 						messages.push({
 							role: "assistant",
 							content: parts,
-							api: "openai-responses",
-							provider: "openai",
-							model: data.model,
+							...identity,
+							providerPayload: {
+								type: "openaiResponsesHistory",
+								provider: identity.provider,
+								dt: true,
+								items: [structuredCloneJSON(item) as unknown as Record<string, unknown>],
+							},
 							usage: {
 								input: 0,
 								output: 0,
@@ -414,7 +441,14 @@ export function parseRequest(body: unknown, headers?: Headers): ParsedRequest {
 					thinkingSignature: JSON.stringify(reasoning),
 					...(reasoning.id ? { itemId: reasoning.id } : {}),
 				};
-				ensureAssistantPlaceholder(messages, data.model, now).content.push(thinking);
+				const assistant = ensureAssistantPlaceholder(messages, data.model, now);
+				assistant.content.push(thinking);
+				appendNativeHistoryItem(assistant, item);
+				continue;
+			}
+			if (effectiveType === "compaction") {
+				const assistant = ensureAssistantPlaceholder(messages, data.model, now);
+				appendNativeHistoryItem(assistant, item);
 				continue;
 			}
 			if (effectiveType === "function_call") {
@@ -436,7 +470,9 @@ export function parseRequest(body: unknown, headers?: Headers): ParsedRequest {
 					arguments: args,
 					...(call.id ? { thoughtSignature: call.id } : {}),
 				};
-				ensureAssistantPlaceholder(messages, data.model, now).content.push(toolCall);
+				const assistant = ensureAssistantPlaceholder(messages, data.model, now);
+				assistant.content.push(toolCall);
+				appendNativeHistoryItem(assistant, item);
 				continue;
 			}
 			if (effectiveType === "custom_tool_call") {
@@ -452,7 +488,9 @@ export function parseRequest(body: unknown, headers?: Headers): ParsedRequest {
 					customWireName: call.name,
 					...(call.id ? { thoughtSignature: call.id } : {}),
 				};
-				ensureAssistantPlaceholder(messages, data.model, now).content.push(toolCall);
+				const assistant = ensureAssistantPlaceholder(messages, data.model, now);
+				assistant.content.push(toolCall);
+				appendNativeHistoryItem(assistant, item);
 				continue;
 			}
 			if (effectiveType === "computer_call") {
@@ -472,7 +510,9 @@ export function parseRequest(body: unknown, headers?: Headers): ParsedRequest {
 						pendingSafetyChecks: call.pending_safety_checks as ComputerSafetyCheck[],
 					},
 				};
-				ensureAssistantPlaceholder(messages, data.model, now).content.push(toolCall);
+				const assistant = ensureAssistantPlaceholder(messages, data.model, now);
+				assistant.content.push(toolCall);
+				appendNativeHistoryItem(assistant, item);
 				continue;
 			}
 			if (effectiveType === "function_call_output") {
@@ -649,13 +689,19 @@ type ComputerCallOutputItem = {
 	pending_safety_checks: ComputerSafetyCheck[];
 	status: "completed" | "in_progress" | "incomplete";
 };
+type CompactionOutputItem = {
+	type: "compaction";
+	encrypted_content: string;
+} & Record<string, unknown>;
+
 
 type OutputItem =
 	| ReasoningOutputItem
 	| MessageOutputItem
 	| FunctionCallOutputItem
 	| CustomToolCallOutputItem
-	| ComputerCallOutputItem;
+	| ComputerCallOutputItem
+	| CompactionOutputItem;
 
 type ResponseStatus = "completed" | "in_progress" | "failed" | "incomplete";
 
@@ -799,6 +845,13 @@ function buildOutputItems(message: AssistantMessage): OutputItem[] {
 		// RedactedThinking / Image are silently dropped — no direct Responses wire representation.
 	}
 	flushMessage();
+	if (message.providerPayload?.type === "openaiResponsesHistory") {
+		for (const item of message.providerPayload.items) {
+			if (item.type === "compaction" && typeof item.encrypted_content === "string") {
+				out.push(structuredCloneJSON(item) as CompactionOutputItem);
+			}
+		}
+	}
 	return out;
 }
 
diff --git a/src/usage.ts b/src/usage.ts
index b7df7550e033c1802038476976adcba63b0ee928..0e1de1254e62137aa9f4b7752bfa5491ae8e29e8 100644
--- a/src/usage.ts
+++ b/src/usage.ts
@@ -173,8 +173,12 @@ export interface UsageCostHistoryEntry {
 	provider: Provider;
 	/** Stable credential identity key (account/email/project/secret derived). */
 	accountKey: string;
-	/** Estimated request cost in USD. */
+	/** Estimated request cost at the model's Standard rate, in USD. */
 	costUsd: number;
+	/** Model that served the request, when recorded by the gateway. */
+	model?: string;
+	/** Effective attributed tier. Only supported ChatGPT Fast requests are priority. */
+	serviceTier?: "default" | "priority";
 }
 
 /** Filter for reading observed request costs. */
